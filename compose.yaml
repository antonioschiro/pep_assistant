services:
  backend-app:
    image: .
    ports:
      - "8000:8000"
    depends_on:
      - model_ai
    volumes:
      - .:/app
  
  model_ai:
    provider:
      type: model
      options:
        model: ai/llama3.2:latest